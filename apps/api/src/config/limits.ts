// Fair-use generation caps, enforced in usage-service.
// Stories reset at the start of each calendar month; audio narrations reset
// at the start of each calendar week (Monday).
export const MAX_STORIES_PER_MONTH = 100;
export const MAX_AUDIO_PER_WEEK = 10;
